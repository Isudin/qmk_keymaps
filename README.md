1. Copy whole dir
2. Modify files (.json file cannot be named keymap.json)
3. qmk flash -kb (keyboardName) -km (path to new directory [CWD is keymaps dir itself])
4. Run the keyboard in flash mode. In case of Corne rev4, mount a keyboard (it works as a drive file)
